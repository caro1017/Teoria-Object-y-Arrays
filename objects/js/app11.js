// object.keys object.values y object.entries

const producto1 = {
    nombre: "Monitor 21 pulgadas",
    precio: 130,
    disponible: true,    
}

console.log( Object.keys (producto1)) // nos va devolver un array con llaves
console.log( Object.values (producto1)) // nos va devolver un array con llaves 
console.log( Object.entries (producto1)) // nos va devolver un array con llaves  
